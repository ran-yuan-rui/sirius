{
  "baseline": {
    "cucascade_sha": "ea9e4a14de0b78bd0f8d78832abaafc29b3a6246",
    "date": "2026-08-01",
    "sirius_sha": "7b7be9c5802c9de3c29f1f5a22ee3169fb87795c"
  },
  "expected": {
    "assertions": 31383,
    "test_cases": 90
  },
  "schema_version": 1,
  "selector": "[s3][integration]~[large]~[aws]",
  "test_names": [
    "sirius_httpfs glob helper throws instead of silently truncating matched files",
    "sirius_httpfs opens through FileOpener and reads positional ranges",
    "sirius_httpfs positional reads fail on short reads and negative sizes",
    "transparent read_parquet over S3 routes through Sirius without httpfs",
    "rest_ioctx lists S3 objects with sizes and follows encoded continuation tokens",
    "rest_ioctx terminates a repeated-token empty LIST loop",
    "rest_ioctx terminates an alternating-token empty LIST loop",
    "rest_ioctx accepts an empty final LIST page and rejects an empty continuation token",
    "rest perf snapshot attributes blocking host reads separately from chunk reads",
    "rest_ioctx paged LIST supports early stop and explicit safety caps",
    "rest_ioctx generated LIST scale obeys configured caps without accumulating pages",
    "rest_ioctx generated LIST scale obeys the default safety caps",
    "rest LIST retries are observable and isolated from object-read byte counters",
    "rest_ioctx opens LIST-sized objects without a HEAD round trip",
    "rest footer suffix parses Content-Range totals",
    "rest footer suffix probe discovers size without HEAD",
    "parquet footer bind is served by one suffix GET and then the stash",
    "footer probe open uses the configured suffix window",
    "footer outside the suffix window falls back to one body GET",
    "describe_parquet over S3 uses footer probe and preserves schema",
    "footer suffix probe falls back safely on unusable suffix responses",
    "footer suffix probe retries transient GET failures",
    "HEAD object-size retries update REST perf counters",
    "REST retry logging includes object keys and stays quiet on clean requests",
    "small objects can be resolved by one suffix probe",
    "concurrent footer probes each get an object-local suffix stash",
    "rest_ioctx reads the MinIO hello fixture through scan_manager create_datasource",
    "rest_ioctx reads exact host ranges and clips EOF on MinIO fixtures",
    "rest_ioctx fans out host_read_ranges against the MinIO medium fixture",
    "rest_ioctx stages device reads through FSMR for single and multi chunk MinIO reads",
    "rest_ioctx retries transient fake-server failures and reports terminal failures",
    "rest_ioctx honors max_connections under concurrent fake range reads",
    "rest_ioctx reads through the TLS MinIO endpoint with the harness CA bundle",
    "rest_ioctx teardown resolves an in-flight async read without hanging",
    "describe_parquet routes S3 parquet through rest_ioctx and returns nation schema",
    "describe_parquet reports stable row counts for multiple S3 parquet objects",
    "describe_parquet maps nested S3 parquet bind shape like DuckDB CPU read_parquet",
    "describe_parquet surfaces missing S3 parquet objects from HEAD",
    "describe_parquet parks parsed parquet metadata in the rest metadata store",
    "describe_parquet reuses the metadata store on repeated S3 binds",
    "describe_parquet footer fetch stays bounded for small and larger S3 parquet objects",
    "gpu_execution rewrites S3 read_parquet and scans through Sirius",
    "transparent read_parquet over S3 scans through Sirius REST",
    "transparent read_parquet over S3 keeps REST routing when local Sirius datasource is off",
    "transparent S3 read_parquet expands globbed parquet files",
    "transparent S3 glob opens the literal percent key instead of its slash decoy",
    "transparent S3 glob opens keys containing URI fragment and query delimiters",
    "transparent S3 glob opens a key containing a literal percent byte",
    "transparent S3 glob decodes a percent-encoded Hive value exactly once",
    "S3 direct and glob routes share the literal object cache identity",
    "transparent S3 non-glob reads distinguish literal percent keys from spaces",
    "transparent S3 glob rejects a literal question mark in a Hive partition segment",
    "transparent S3 glob rejects a question mark before the Hive partition separator",
    "transparent S3 glob permits a question mark in the terminal filename",
    "transparent S3 glob supports an encoded question mark in a Hive partition value",
    "S3 glob results are sorted by raw literal key bytes",
    "transparent S3 glob ignores percent-encoded keys outside the match",
    "transparent S3 glob preserves hive partition columns",
    "transparent S3 glob scans use parquet footer probes",
    "transparent S3 glob remains correct with a straddled footer-probe window",
    "transparent S3 glob warm scan skips blocking host reads",
    "transparent S3 glob matcher semantics match DuckDB segment globs",
    "transparent S3 glob reports no-files and GPU-only errors clearly",
    "gpu_execution S3 SQL surface returns empty result sets cleanly",
    "S3 pushdown shape-C zero-side joins match the local parquet oracle",
    "gpu_execution S3 SQL surface counts every uploaded TPCH parquet table",
    "gpu_execution S3 SQL surface scans all orders row groups",
    "gpu_execution S3 SQL surface matches local TPC-H Q1 shape",
    "gpu_execution S3 SQL surface matches local TPC-H Q3 shape",
    "S3 read_parquet op shape T1 matches outer join semantics",
    "S3 read_parquet op shape T2 matches grouped distinct aggregates",
    "S3 read_parquet op shape T3 matches string predicates",
    "S3 read_parquet op shape T4 mixes local and S3 scans",
    "S3 read_parquet op shape T5 preserves null decimal and timestamp values",
    "S3 read_parquet op shape T6 matches CTE and scalar subquery results",
    "gpu_execution S3 nested parquet projections match local DuckDB CPU",
    "gpu_execution rejects operations on nested S3 parquet columns cleanly",
    "transparent S3 window query reports unsupported S3 CPU fallback",
    "transparent S3 projection fallback reports prose instead of an exception envelope",
    "transparent S3 view fallback is rejected instead of replaying on CPU",
    "S3 read_parquet is rejected when transparent GPU execution is disabled",
    "internal sirius_read_parquet bind returns row-count metadata for cardinality",
    "internal sirius_read_parquet exposes S3 row count to DuckDB EXPLAIN",
    "internal sirius_read_parquet exposes distinct S3 table cardinalities in joins",
    "gpu_execution S3 SQL telemetry reaches the REST ioctx",
    "gpu_execution S3 SQL supports both configured SigV4 signing modes",
    "gpu_execution S3 SQL works over TLS with the harness CA bundle",
    "gpu_execution S3 SQL preserves correctness with prefetch cache disabled",
    "gpu_execution S3 SQL survives a constrained GPU memory config with disk tier",
    "transparent S3 TPC-H Q1-Q22 match the tiny local CPU oracle"
  ]
}
